{
  "name": "decad.sol",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "solhint \"./contracts/**/*.sol\"",
    "compile": "truffle compile",
    "fmt": "prettier --write --plugin=prettier-plugin-solidity contracts/**/*.sol",
    "ganache-server": "ganache-cli -p 18545 --deterministic",
    "migrate": "truffle migrate --network development",
    "console": "truffle console --network development"
  },
  "keywords": [],
  "author": "Etaoin",
  "license": "MIT",
  "devDependencies": {
    "ganache-cli": "^6.12.2",
    "prettier": "^3.0.0",
    "prettier-plugin-solidity": "^1.1.3",
    "solhint": "^3.4.1",
    "truffle": "^5.11.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}
